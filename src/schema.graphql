type Query {
  test: String!
  mostSearched: [MostSearchedCats]
}

type Mutation {
  search(entry: String!): [CatSuggested!]
}

type CatSuggested {
  id: String!
  catName: String!
}

type MostSearchedCats {
  id: Int!
  originalId: String!
  name: String!
  timesSearched: Int!
}